{#
/**
 * @file
 * Default theme implementation for a breadcrumb trail.
 *
 * Available variables:
 * - breadcrumb: Breadcrumb trail items.
 *
 * @ingroup templates
 */

 Rod Tatham, Sereno :: 2019-10-01
  - hidden on the home page
  - pinched in with container
#}
{% if breadcrumb and not is_front %}
<div class="container">
  <ol class="breadcrumb">

    {% for item in breadcrumb %}
      <li {{ item.attributes }}>
        {% if item.url %}
          <a href="{{ item.url }}">{{ item.text }}</a>
        {% else %}
          {{ item.text }}
        {% endif %}
      </li>
    {% endfor %}

  </ol>
  </div>
{% endif %}
